/*
rule = CoffeeChatRule
 */
object NamingSchemeChange {
    def complete(isSuccess: Boolean): Unit = ()
    complete(true)
    val myOldName = true
    println(true)
    def finish(isError: Boolean): Unit = ()
    finish(true)
}
